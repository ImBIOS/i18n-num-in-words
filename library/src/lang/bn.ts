import { numInWordsFactory } from '~/utils/num-in-words-factory';

const bn = (num: number): string => {
  return 'Not implemented yet!';
};

export const bengaliNumInWords = numInWordsFactory(bn, {
  lang: 'bn',
  status: 'alpha',
});
