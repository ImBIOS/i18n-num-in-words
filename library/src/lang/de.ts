import { numInWordsFactory } from '~/utils/num-in-words-factory';

const de = (num: number): string => {
  return 'Not implemented yet!';
};

export const germanNumInWords = numInWordsFactory(de, {
  lang: 'de',
  status: 'alpha',
});
