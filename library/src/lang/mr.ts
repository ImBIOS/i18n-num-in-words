import { numInWordsFactory } from '~/utils/num-in-words-factory';

const mr = (num: number): string => {
  return 'Not implemented yet!';
};

export const marathiNumInWords = numInWordsFactory(mr, {
  lang: 'mr',
  status: 'alpha',
});
